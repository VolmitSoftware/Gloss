package art.arcane.gloss.bedrock;

/** The existing surfaces a Bedrock viewer may be excluded from; see [bedrock] in gloss.toml. */
public enum BedrockSurface {
    HOLOGRAM,
    PANEL,
    BUBBLE,
    INDICATOR,
    DROP,
    OVERLAY
}
